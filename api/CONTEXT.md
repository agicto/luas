# Luas Context

The canonical Luas glossary now lives at [`../CONTEXT.md`](../CONTEXT.md).

Keep this file as a compatibility pointer for agents and docs that still look inside `api/`. Do not add API-only vocabulary here unless it is deliberately excluded from the global glossary.
