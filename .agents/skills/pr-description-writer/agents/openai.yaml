interface:
  display_name: "PR Description Writer"
  short_description: "Package a diff for review"
  default_prompt: "Use $pr-description-writer to turn this completed diff into a reviewable PR description."
policy:
  allow_implicit_invocation: false
