interface:
  display_name: "Verification Before Completion"
  short_description: "Resolve an unclear proof scope"
  default_prompt: "Use $verification-before-completion to identify sufficient proof when the nearest verification matrix is unclear."
